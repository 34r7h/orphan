{
  "name": "orphan-platform-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for the Orphan Platform",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:coverage": "hardhat coverage",
    "deploy:testnet": "hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network base",
    "verify:testnet": "hardhat verify --network baseSepolia",
    "verify:mainnet": "hardhat verify --network base",
    "clean": "hardhat clean",
    "node": "hardhat node",
    "console": "hardhat console"
  },
  "keywords": [
    "ethereum",
    "smart-contracts",
    "nft",
    "blockchain",
    "hardhat",
    "solidity"
  ],
  "author": "Orphan Platform Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@types/node": "^20.11.0",
    "hardhat": "^2.19.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.6",
    "dotenv": "^16.3.0"
  }
}
