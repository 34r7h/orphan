# Fix npm resolution issues
legacy-peer-deps=true
package-lock=false
save-exact=false
engine-strict=false

# Prevent npm from looking at parent directories
global-style=false
save-prefix=^

# Registry configuration
registry=https://registry.npmjs.org/

# Logging
loglevel=warn

# Performance
prefer-offline=true
audit=false
fund=false
